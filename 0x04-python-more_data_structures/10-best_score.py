#!/usr/bin/python3
def best_score(a_dictionary):
    max_num = max(a_dictionary.values())
    for key, value in dictionary.items():
        if value == certain_value:
            return key
        else:
            return None

